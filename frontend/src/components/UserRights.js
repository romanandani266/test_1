import React from "react";
import { Container, Typography } from "@mui/material";

function UserRights() {
  return (
    <Container>
      <Typography variant="h4">User Rights</Typography>
      <Typography>
        Implement user rights functionality here (e.g., Right to Know, Right to Delete, etc.).
      </Typography>
    </Container>
  );
}

export default UserRights;
